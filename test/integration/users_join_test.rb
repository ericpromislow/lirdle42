require "test_helper"

# Test users coming in with a generated invitation link

class UsersJoinTest < ActionDispatch::IntegrationTest
  def setup
    @user = users(:user1)
  end
#   test "generate a link" do
#     log_in_as(@user1)
#     get root_url
#     post invitations_path, params: { from: @user1.id, to: @user2.id }
#     
#   end

#  test "join with a url" do
    

end
